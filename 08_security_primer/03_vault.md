# Vault

Hashicorp vault can be used for 
- storing aws secert , access key
- storing passwords, api tokens,
- Vault can also help in rotating the keys at regular intervals.
- It can also set expiry duration

We can store any thing in vault and use it in terraform
